# 曲曲分身｜Codex 接手指南

本仓库是“曲曲分身”情感关系对话应用。目标不是检索式问答，而是以已授权的直播与课程语料沉淀出的关系判断、操作方法、案例识别、追问方式和口语节奏，驱动直接、可执行的多轮情感对话。

## 快速运行

在仓库根目录执行 `pnpm install`、`pnpm dev`。执行 `pnpm test && pnpm check` 可运行全部自动化测试及 TypeScript 类型检查。不要提交 `.env` 或任何凭据；生产环境的数据库、鉴权和模型环境变量由托管平台注入。

## 核心架构

| 位置 | 作用 |
| --- | --- |
| `client/src/pages/Home.tsx` | 主工作台，含新建对话、快捷情境、审核台与语料证据页。 |
| `client/src/lib/directMessages.ts` | 本地消息队列。用户消息即时进入视图；模型完整回复一返回即直接追加，避免等待历史列表重新拉取。 |
| `client/src/components/AIChatBox.tsx` | 聊天显示、输入与轻量生成状态。 |
| `server/routers.ts` | tRPC 路由。`chat.send` 先处理风险，再以人格底稿和会话历史调用模型。 |
| `server/agentPolicy.ts` | 高风险内容分流与人格系统提示词组装。 |
| `server/personaBlueprint.ts` | 运行时人格底稿、RICH 关系判断顺序及十类行动方法库。修改人格时优先审阅此文件。 |
| `server/db.ts` | 语料、版本、会话和审核数据访问。 |
| `drizzle/schema.ts` | Drizzle 数据模型。更改后必须生成并审阅迁移，再执行迁移。 |

## 当前人格与产品约束

人格须保持**口语化、短句、略急促、直接但不羞辱**的表达。回答优先遵循“核心矛盾 → 立即操作 → 验证标准 → 一个关键追问”。禁止退化为泛化安慰、原文检索展示、长篇报告、编造语料没有的金额／期限／案例结果，或使用操控、威胁、羞辱性表达。

对自伤他伤、暴力控制、医疗诊断与法律纠纷，必须保留 `server/agentPolicy.ts` 中的安全优先分流；常规人格指令不得覆盖该逻辑。

## 语料版本与完整性

默认学习语料版本为 `2026-08-28-v2`，记录于 `corpus_versions` 和 `corpus_version_sources`：

| 新版别名 | 实际来源 | 处理方式 | 已核验文本行数 |
| --- | --- | --- | ---: |
| `live-2022-2026-v2` | `live-2022-2026-complete` | 新增文件严格顺序逐行导入 | 403,052 |
| `course-1-23-v2` | `course-1-23` | 更新文件哈希一致，采用独立版本别名复用 | 179,083 |

新版语料被切成 46 个连续分块，产出 293 张原文锚定人格卡片，并聚合为当前运行时人格方法库。原始行、章节、段落、语义单元及审核状态存于数据库，不应复制到浏览器侧或 Git 仓库。

导入脚本为 `scripts/ingest-corpus.mjs`。此脚本会对指定来源重建其分段与语义单元，只有在确需重新导入且已核验源文件哈希、字节数及行数时才运行。不要为测试目的插入或删除用户会话、原文或审核数据。

## 后续迭代建议

修改对话速度时，优先维持以下原则：不在模型调用前做全文检索、模型目录查询或串行无关数据库查询；将历史读取和用户消息持久化并行；前端不等待 `messages` 查询失效后才显示模型返回结果。当前模型由服务端调用，客户端不得携带任何模型密钥。

为新功能补充 Vitest。涉及消息呈现，至少覆盖“即时用户消息”“完整回答直达”“失败回滚”“会话切换隔离”。涉及语料版本，至少覆盖默认版本、来源映射、行数、章节、段落及语义单元追溯。
