# 给 Codex 的任务上下文

请先阅读根目录 `AGENTS.md`。当前优先目标是持续提高“曲曲分身”的人格一致性与对话操作性，**不是**把它做成展示原文出处的检索助手。

当前代码已经完成新版语料版本接入、人格方法库、对话本地即时展示和安全分流。开始修改前请运行：

```bash
pnpm test && pnpm check
```

若涉及数据库结构，遵循：编辑 `drizzle/schema.ts` → `pnpm drizzle-kit generate` → 审阅生成 SQL → 应用迁移 → 补充测试。禁止提交凭据、原始授权语料或数据库导出文件。

每次迭代优先保留以下行为：用户消息本地即时显示；输入框不得因回答生成而锁住；助手回复生成一段显示一段；上一条回复仍在生成时用户仍可继续发送；失败时保留已经发出的用户消息并在对应助手气泡中显示错误。高风险安全分流不可删除。

## 即时聊天与流式输出（2026-09-13）

- 串题修复：`server/conversationContext.ts` 在明确引入新人物／新案例时切分模型与规则所用历史，后续省略追问保留该案例上下文。该识别目前是启发式，不能声称覆盖所有隐式换题。已补连续“供养停播 → 朋友娱乐直播 → 她没才艺”回归测试；供养回复已去掉写死的金额、城市、停留期限。未知问题的兜底不得再默认输出分手复合模板。
- 实际接口验收已不再把六七万案例用于朋友直播题。项目所有者明确认可“别急着找大哥”这种直接给行动顺序的口语，不能因为没有复述“抗拒爱情大哥”就判为答错；此前代理对此的负面判断已撤回。仍需检查是否回应当前目标、有无重复劝告和解释不足。串题修复不等于人格表达验收通过。
- 表达校准优先沿用原文常用词、句式、排比和语气，避免把原文全改写成抽象咨询概念。本地检索优先返回命中处的完整发言段（限制长度），让生成看到上下句的推进；原文仍留在本地私有索引和本地提示中。开头尽快给直接答案，展开原因，结尾讲后续；不得机械要求先肯定或固定三条。

- 游客试聊通过 `POST /api/chat/guest-stream` 返回 NDJSON 流，`server/streamingChat.ts` 负责安全分流、已核对规则和本地模型流式回调。不要把它改回等待完整字符串后再展示的假流式动画。
- `client/src/pages/Home.tsx` 发送时先乐观插入一对用户／助手消息；`client/src/lib/streamingChat.ts` 逐行解析 `delta`；`client/src/lib/directMessages.ts` 只更新这次请求对应的助手占位消息，所以并发发送不会串到同一个气泡。
- `client/src/components/AIChatBox.tsx` 只在输入为空时禁用发送；网络请求进行中仍保持输入和建议问题可用。助手尚未收到首段时显示等待点，收到流后显示光标。
- 开发环境启动服务器时会预热本地语气模型，减少第一问加载权重的等待。`scripts/local-voice-chat.py` 的流式路径使用单次生成并逐段发回；高置信、已核对案例仍由生成前规则优先命中。
- 登录态保存对话当前同样采用乐观消息和不锁输入框，但原有 tRPC 服务端仍是整段返回；若以后恢复登录功能并要求同等逐段输出，应新增带身份校验和落库事务的流式端点，不能只在前端模拟打字。

## 本地授权语料管线

原始课程与直播文本不得复制进仓库。当前开发机把逐行索引、MLX 环境、模型权重和分析结果放在仓库同级的 `.ququ-private-corpus/`；该目录不属于 Git 工作树。

- `scripts/build-local-voice-index.mjs`：先按固定字节数与 SHA-256 核验两个源文件，再保存每一行、行哈希、连续区块、语气标记与检索索引。
- `scripts/build-deterministic-voice-profile.mjs`：当前全量覆盖的权威分析路径。它扫描全部 582,135 行、4,935,220 个正文字符和 48,514 个发言段，统计语气句法、主题与原文证据范围，并写回私有 SQLite。当前分析版本 `deterministic-v1`，内容哈希 `24ad6d31fe11ebf50a82859f84da3dea819340254ac86e8554477d3f9b58b36a`。
- `server/corpusDerivedVoiceProfile.ts`：只保存上述全量扫描的统计、转述规则和证据定位，不保存授权原文。运行时人格版本是 `2026-09-08-full-corpus-v1`。
- `scripts/extract-local-voice.py`：可选的离线 MLX 语义增强路径。`voice-chunk-v6` 共 495 个区块；2026-09-10 最近一次检查为 83 complete、3 failed、407 pending、2 running。提取进程为避免和页面试聊争抢同一块 GPU 已暂停，可续跑并重试失败项；不得把这条未完成的增强路径写成“全量完成”。
- `scripts/distill-local-voice.py`：只有 v6 全量完成时才允许生成 `voice-profile-v6`；任何漏段或失败都会拒绝蒸馏。它不是当前全量覆盖的前置条件。
- `scripts/local-voice-chat.py` 与 `server/localVoiceClient.ts`：开发环境无云模型密钥时，优先使用本机 4B 模型、私有检索、全量确定性统计和已完成的 v6 增强结果生成连续回答；MLX 不可用时回落到已由全量语料规则校准的确定性回复。
- `server/reviewedJudgmentRules.ts`：位于模型前后的高置信判断层。项目所有者已经明确核对“无附加义务的补偿礼物可以先收，但礼物和复合分账；长期问题未解决仍不复合”。该精确案例未在两份原始文本中逐字命中，因此规则来源须标为“所有者核对 + 相关语料原则支持”，不能伪称逐字引用。高置信场景先命中规则再调用模型；其余模型回答若编造对话中没有的金额、期限，或只连续反问而不给判断，则回落到确定性回答。

本地处理必须设置 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`。不要把原文、SQLite、模型缓存或失败输出上传到外部推理服务。

## 当前产品验证用例

连续对话至少覆盖以下行为：第一轮同时出现长期不生育、照护责任、城市／家庭资源、五年投入和分手后贵重礼物时，先判断是否复合；第二轮只问“礼物收不收、退不退”时，必须承接前文，以“先收着，不退”开头，不得重新输出第一轮模板。核心口径是“礼物和复合是两笔账”：礼物可以收，但不能因此视为原谅、翻篇或答应复合；长期问题没有解决，就仍然不能复合。

还须覆盖省略指代：在前文已谈到礼物时，追问“如果他说收了就必须复合呢”必须识别为同一笔礼物，并回答“先不收，退回去”；原因是这时礼物附带了关系义务，已从回补变成购买选择权。

## 人格表达校准（2026-09-10）

2026-09-13 更新：已新增仓库技能 `skills/ququ-voice-calibration/SKILL.md`，本机可安装到 `~/.codex/skills/ququ-voice-calibration/`。`shared/voiceDelivery.json` 是本地生成与服务端共同读取的表达规则。两份所有者参考的短节选留在私有目录 `owner-voice-examples.json`，没有提交原文。校准优先保留日常词和句式，开头给方向、解释原因、结尾讲后续，不固定先肯定或列三条。

本地游客试聊的供养案例现在把已核准原则作为约束传给模型生成，其他环境仍保留原有规则路径；模型失败仍有兜底。生成上限从 520 调至 1600 tokens，增加重复惩罚。三轮实际生成并未达到表达验收：日常措辞有改善，但仍有动机武断、准备阶段变成已开始、账号归属混淆和擅自引导带货。原始两题与追加追问、具体差距记录在同级私有目录 `voice-calibration-2026-09-13.md`，不得声称人格校准已完成。

项目所有者明确要求“道先于术”：复杂问题不能压成通用的“结论 + 三条动作”，约三分之二篇幅应先讲透交换结构、价值类型与长期后果，最后才给一至三步直接行动，并逐项解释原因。简单问题目标约 300–600 个中文字符，事实多、交换复杂的问题约 650–1,200 个中文字符；语言应像真实口头拆解，可用短反问后自行回答，不要写成整齐的 AI 报告。

当前新增的跨情境底层价值函数是“可撤销供养不能交换可积累能力”：能力、账号、粉丝、客户、人脉和独立生活盘长在用户身上；转账、供养、对方安排的住房与临时承诺掌握在别人手里。后者可以接住，但不能以停工、迁城、切断社交和失去退出权为代价。验证用例覆盖“主播 + 大哥转账上涨 + 要求停播 + 迁城租房”：回答必须先说“钱可以收、直播不要停、原城市生活盘不能撤”，主体解释为什么，且不得退回“复合／礼物”模板。
